<Window x:Class="RenderFarm.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RenderFarm Launcher"
        Width="980"
        Height="720"
        MinWidth="860"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="#101823">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#162131" />
        <SolidColorBrush x:Key="PanelBrush" Color="#1D2A3A" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="#223145" />
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#314258" />
        <SolidColorBrush x:Key="TextBrush" Color="#E8EEF7" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#9AA9BC" />
        <SolidColorBrush x:Key="AccentBrush" Color="#4FD1C5" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#6EE7DD" />
        <SolidColorBrush x:Key="LavenderBrush" Color="#A7A6FF" />
        <SolidColorBrush x:Key="AmberBrush" Color="#F2C46D" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,5,0,14" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Background" Value="#121C29" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="#0B1520" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.84" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#28384C" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="22" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="RenderFarm Launcher" FontSize="28" FontWeight="SemiBold" />
                    <TextBlock Text="Start the controller dashboard or connect this machine as a render worker." Foreground="{StaticResource MutedTextBrush}" Margin="0,7,0,0" />
                </StackPanel>
                <Border Grid.Column="1" Background="#182B34" BorderBrush="#315E63" BorderThickness="1" CornerRadius="14" Padding="13,8" VerticalAlignment="Center">
                    <TextBlock Text="Ready for production setup" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" />
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="315" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel>
                    <TextBlock Text="Machine role" FontSize="18" FontWeight="SemiBold" />
                    <TextBlock Text="Choose the responsibility for this PC. Settings are stored per user and can be changed later." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,8,0,20" />

                    <RadioButton x:Name="ControllerRole" Content="Controller" FontWeight="SemiBold" Checked="RoleChanged" />
                    <TextBlock Text="Runs the dashboard, queue, database, and scheduler." Foreground="{StaticResource MutedTextBrush}" Margin="24,0,0,18" TextWrapping="Wrap" />

                    <RadioButton x:Name="WorkerRole" Content="Worker" FontWeight="SemiBold" Checked="RoleChanged" />
                    <TextBlock Text="Runs Unreal render jobs assigned by a controller." Foreground="{StaticResource MutedTextBrush}" Margin="24,0,0,22" TextWrapping="Wrap" />

                    <Border Height="1" Background="{StaticResource BorderBrushSoft}" Margin="0,0,0,16" />
                    <TextBlock Text="Settings file" FontWeight="SemiBold" />
                    <TextBlock x:Name="SettingsPathText" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,6,0,0" />
                </StackPanel>
            </Border>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border x:Name="ControllerPanel" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Controller settings" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="The default dashboard address is local-only. Use 0.0.0.0 only when workers on the LAN need to connect." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,6,0,16" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="150" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Host name or IP" FontWeight="SemiBold" />
                                    <TextBox x:Name="HostNameBox" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Port" FontWeight="SemiBold" />
                                    <TextBox x:Name="PortBox" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border x:Name="WorkerPanel" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Worker settings" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="Point this machine at a controller. Render paths, Unreal executables, and output locations are managed from the controller dashboard." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,6,0,16" />

                            <TextBlock Text="Controller URL" FontWeight="SemiBold" />
                            <TextBox x:Name="ControllerUrlBox" />

                            <CheckBox x:Name="DiscoveryEnabledBox" Content="Allow LAN discovery when Controller URL is blank" />

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Worker ID" FontWeight="SemiBold" />
                                    <TextBox x:Name="WorkerIdBox" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Display name" FontWeight="SemiBold" />
                                    <TextBox x:Name="DisplayNameBox" />
                                </StackPanel>
                            </Grid>
                            <StackPanel Visibility="Collapsed">
                                <TextBlock Text="Legacy Unreal search root" FontWeight="SemiBold" />
                                <TextBox x:Name="UnrealSearchRootBox" />

                                <TextBlock Text="Legacy project path" FontWeight="SemiBold" />
                                <TextBox x:Name="ProjectPathBox" />

                                <TextBlock Text="Legacy shared output root" FontWeight="SemiBold" />
                                <TextBox x:Name="SharedOutputRootBox" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Security" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="API token" FontWeight="SemiBold" Margin="0,14,0,0" />
                            <TextBox x:Name="ApiTokenBox" />
                            <TextBlock Text="Leave blank for trusted local testing. If the controller requires a token, use the same value on every worker." Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Background="#13232C" BorderBrush="#2F5C62" BorderThickness="1" CornerRadius="12" Padding="18">
                        <StackPanel>
                            <TextBlock Text="Status" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" />
                            <TextBlock x:Name="StatusHeadlineText" Text="Ready." FontSize="18" FontWeight="SemiBold" Margin="0,8,0,0" />
                            <TextBlock x:Name="StatusDetailsText" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="12" Padding="14" Margin="0,18,0,0">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="StartRoleButton" Content="Start selected role" Click="StartRole_Click" />
                    <Button x:Name="OpenDashboardButton" Content="Open dashboard" Style="{StaticResource SecondaryButton}" Click="OpenDashboard_Click" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <TextBlock Text="Controller dashboard: http://127.0.0.1:9200/" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
